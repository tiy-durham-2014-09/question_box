<%= form_for(@answer) do |f| %>
  <p>
    <%= f.hidden_field :user, :value => @current_user %>
  </p>
  <p>
    <%= f.label :text %>
    <%= f.text_area :text %>
  </p>
  <p>
    <%= f.submit { redirect_to root_path }%>
  </p>
  <% end %>
