<h2>Answer this question:</h2>
<%= form_for @answer do |f| %>

  <%= f.text_area :text, rows: 5, placeholder: "Answer this question" %>
  <br>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.hidden_field :question_id, value: @question.id %>
  <%= f.submit "Submit" %>
<% end %>