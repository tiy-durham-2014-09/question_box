<h2>All Questions</h2>
<% sorted_questions = @questions.sort_by(&:created_at).reverse %>
<% sorted_questions.each do |q| %>
  <%= link_to q.title, question_path(q.id) %> | <%= q.user.name %> | <%= q.created_at.to_date %>
  <br>
<% end %>