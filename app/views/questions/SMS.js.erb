$("#<%= dom_id(@question) %>").effect("highlight", {color: '#AAA68B'}, 1000);
$.ajax({
  url: "",
  context: document.body,
  success: function(s,x){
    $(this).html(s);
  }
});

//$("#<%= dom_id(@question) %>").val("");

//$("#<%= dom_id(@question) %>").update();


