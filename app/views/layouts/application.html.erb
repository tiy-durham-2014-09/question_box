<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>QuestionBox</title>
	  <!--  content_for?(:title) ? yield(:title) : "foundation-rails"  -->
    <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet">
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

  <nav class="top-bar" data-topbar role="navigation">
    <ul class="title-area">
      <li class="name">
        <h1><%= link_to 'QuestionBox', root_path %></a></h1>
      </li>
      <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
      <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
    </ul>

    <section class="top-bar-section">
      <!-- Right Nav Section -->
      <ul class="right">
        <% if logged_in? %>
          <li><a>Logged in as <%= current_user.name %></a></li>
          <li class="divider"></li>
          <li><%= link_to "Logout", login_path, :method => :delete %></li>
        <% else %>
          <li><%= link_to "Login", new_login_path %></li>
        <% end %>

        <li class="divider"></li>
        <li class="has-form">
          <%= link_to "Ask Question", root_path, :class => "button" %>
        </li>
      </ul>

      <!-- Left Nav Section -->
      <ul class="left">
        <li class="divider"></li>
        <li><%= link_to 'All questions', questions_path %></li>
      </ul>
    </section>
  </nav>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>